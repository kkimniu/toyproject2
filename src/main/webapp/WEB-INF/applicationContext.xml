<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xmlns:tx="http://www.springframework.org/schema/tx"

	   xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/tx
	http://www.springframework.org/schema/tx/spring-tx.xsd">

	<!-- configs -->
	<context:property-placeholder location="classpath:application.properties"/>

	<!-- Controller 제외: 서비스/리포지토리/컴포넌트만 스캔 -->
	<context:component-scan base-package="com.roommate">
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
		<context:exclude-filter type="annotation" expression="org.springframework.web.bind.annotation.RestController"/>
	</context:component-scan>

	<!-- mybatis configs -->
	<!-- 1) dataSource -->
	<bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
		<property name="driverClassName" value="${db.driver}"/>
		<property name="url"             value="${db.url}"/>
		<property name="username"        value="${db.username}"/>
		<property name="password"        value="${db.password}"/>
	</bean>
	<!-- 2) 트랜젝션관리자 등록 -->
	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource"></property>
	</bean>
	<!-- ✅ 트랜잭션 어노테이션 활성화 -->
	<tx:annotation-driven transaction-manager="transactionManager"></tx:annotation-driven>

	<!-- 3) 마이바티스 설정 : sql객체 만들어주는 공장 -->
	<bean id="sqlSessionFactoryBean" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="classpath:/mybatis-config.xml" />
		<!-- ✅ 누락되면 매퍼/별칭 인식 실패 -->
		<property name="mapperLocations" value="classpath*:mapper/**/*.xml"/>
		<property name="typeAliasesPackage" value="com.roommate.domain.**.entity"/>
	</bean>

	<!-- MyBatis @Mapper 인터페이스 자동 스캔 -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<!-- @Mapper 인터페이스가 들어있는 패키지 -->
		<property name="basePackage" value="com.roommate.domain.**.repository"/>
		<!-- @Mapper 붙은 인터페이스만 대상으로 (권장) -->
		<property name="annotationClass" value="org.apache.ibatis.annotations.Mapper"/>
		<!-- 위에서 만든 SqlSessionFactoryBean 이름과 일치 -->
		<property name="sqlSessionFactoryBeanName" value="sqlSessionFactoryBean"/>
		<!-- (선택) SqlSessionTemplate를 지정하려면 아래 사용
        <property name="sqlSessionTemplateBeanName" value="sqlSessionTemplate"/>
        -->
	</bean>
	<!-- 객체를 대여해줄 수 있는 비디오가게 -->
	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg name="sqlSessionFactory" ref="sqlSessionFactoryBean"></constructor-arg>
	</bean>
</beans>
